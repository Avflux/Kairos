<!-- Menu dividido em componentes modulares -->
<div class="menu-container">
    <!-- Seção Logo (Topo) -->
    <LogoMenu />

    <!-- Seção Botões/Menu Items (Meio) -->
    <BotonMenu />

    <!-- Seção Perfil (Fundo) -->
    <PerfilMenu />
</div>
